---
import { cn } from "@/lib/cn";

interface Props {
  level?: "h1" | "h2";
  class?: string;
}

const { level = "h1", class: className } = Astro.props;
const Tag = level;
---

<Tag class={cn("mb-6 text-3xl font-bold text-gray-900 md:text-4xl dark:text-gray-100", className)}>
  <slot />
</Tag>
