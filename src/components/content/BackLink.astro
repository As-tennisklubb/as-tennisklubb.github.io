---
import { cn } from "@/lib/cn";

interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a href={href} class={cn("link mb-6 inline-flex items-center gap-1 text-sm font-medium", className)}>
  <slot />
</a>
